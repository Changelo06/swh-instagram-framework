{
  "name": "swh-instagram-framework",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "SWH Instagram Framework Builder — one-click launcher orchestrates the client build + production server.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "npm run setup && npm run build && node start.js",
    "start:fast": "node start.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  }
}
